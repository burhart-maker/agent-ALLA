services:
  - type: web
    name: alla-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ALLA_MODEL
        value: claude-sonnet-4-6
