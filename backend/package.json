{
  "name": "alla-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Alla backend — real API server (not the sample() Claude-artifact capability), serves the public site, the embeddable widget, and /api/chat.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
